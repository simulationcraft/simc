action_priority_list_t* precombat = get_action_priority_list( "precombat" );
action_priority_list_t* def = get_action_priority_list( "default" );

precombat->add_action( "moonkin_form","Snapshot raid buffed stats before combat begins and pre-potting is done." );
precombat->add_action( "wrath" );
precombat->add_action( "wrath" );
precombat->add_action( "starfire,if=!talent.stellar_flare" );
precombat->add_action( "stellar_flare" );

def->add_action( "use_items","Executed every time the actor is available." );
def->add_action( "auto_attack" );
def->add_action( "moonfire,target_if=refreshable" );
def->add_action( "sunfire,target_if=refreshable" );
def->add_action( "stellar_flare,target_if=refreshable" );
def->add_action( "force_of_nature" );
def->add_action( "fury_of_elune" );
def->add_action( "incarnation" );
def->add_action( "celestial_alignment" );
def->add_action( "warrior_of_elune,if=!talent.lunar_calling&buff.eclipse_solar.remains<7|talent.lunar_calling" );
def->add_action( "starfire,if=(!talent.lunar_calling&spell_targets.starfire=1)&(buff.eclipse_solar.up&buff.eclipse_solar.remains<action.starfire.cast_time|eclipse.in_none)" );
def->add_action( "wrath,if=(talent.lunar_calling|spell_targets.starfire>1)&(buff.eclipse_lunar.up&(buff.eclipse_lunar.remains<action.wrath.cast_time)|eclipse.in_none)" );
def->add_action( "starfall,if=buff.starweavers_warp.up" );
def->add_action( "starsurge,if=spell_targets.starfall<2" );
def->add_action( "starfall,if=spell_targets.starfall>1" );
def->add_action( "convoke_the_spirits" );
def->add_action( "new_moon" );
def->add_action( "half_moon" );
def->add_action( "full_moon" );
def->add_action( "warrior_of_elune" );
def->add_action( "wild_mushroom" );
def->add_action( "starfire,if=talent.lunar_calling|buff.eclipse_lunar.up&spell_targets.starfire>1" );
def->add_action( "wrath" );
