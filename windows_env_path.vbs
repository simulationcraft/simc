'Read the the current working directorySet fso = CreateObject("Scripting.FileSystemObject")Set cwd = fso.GetFolder(".")'Stores it in the %SimcPath% variableSet wshShell = WScript.CreateObject("WScript.Shell") Set wshEnv = WshShell.Environment("User") wshEnv("SimcPath") = cwd'Does the path variable already contains %simcpath ?If Not(InStr(wshEnv("Path"), "%SimcPath%") > 0) Then	If Len(path) = 0 Then		wshEnv("Path") = "%SimcPath%"	Else		wshEnv("Path") = wshEnv("Path") & ";%SimcPath%"	End IfEnd If